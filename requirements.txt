# JOJO Trading Command Center — Python dependencies
#
# Note: tensorflow was previously listed but nothing imported it, and it is a
# ~600MB install. The strategies use plain numpy.

# Server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
websockets>=12.0

# Market data & execution
ccxt>=4.2.0

# Analysis
numpy>=1.24.0
pandas>=2.0.0

# Persistence
aiosqlite>=0.19.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
